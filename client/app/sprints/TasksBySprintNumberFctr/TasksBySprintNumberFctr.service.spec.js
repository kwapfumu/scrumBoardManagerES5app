'use strict';

describe('Service: TasksBySprintNumberFctr', function () {

  // load the service's module
  beforeEach(module('scrumBoardEs5AppApp'));

  // instantiate service
  var TasksBySprintNumberFctr;
  beforeEach(inject(function (_TasksBySprintNumberFctr_) {
    TasksBySprintNumberFctr = _TasksBySprintNumberFctr_;
  }));

  it('should do something', function () {
  });

});
