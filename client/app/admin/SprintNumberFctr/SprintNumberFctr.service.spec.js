'use strict';

describe('Service: SprintNumberFctr', function () {

  // load the service's module
  beforeEach(module('scrumBoardEs5AppApp'));

  // instantiate service
  var SprintNumberFctr;
  beforeEach(inject(function (_SprintNumberFctr_) {
    SprintNumberFctr = _SprintNumberFctr_;
  }));

  it('should do something', function () {
  });

});
